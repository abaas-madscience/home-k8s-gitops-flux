apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openebs-perf-test-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: openebs-hostpath # or your OpenEBS storage class
  resources:
    requests:
      storage: 5Gi
