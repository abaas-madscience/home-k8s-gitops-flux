apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  - helmrepo.yaml

configMapGenerator:
  - name: prom-stack-values
    namespace: prom-stack
    files:
      - values.yaml=stack-values.yaml
