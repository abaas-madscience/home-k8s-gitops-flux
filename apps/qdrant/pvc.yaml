---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qdrant-database-storage
  namespace: qdrant
spec:
  storageClassName: openebs-database
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 75G
